language: node_js
node_js:
  - "10"

jobs:
  include:
    - stage: test
      name: "test"
      script: yarn test
    - name: "Echo for check"
      script: echo 'If installing and testing has done, it is OK.'